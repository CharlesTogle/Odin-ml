"""Shared, canonical ML building blocks for Odin-ML.

Single source of truth for the model artifact classes (consumed by both the
training pipeline under `training/` and the serving layer under `app/`), and
for reproducibility helpers that emit `metadata.json`.
"""
